<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Classic VSSDK VSIX project targeting Visual Studio 17.x.
    Uses the modern SDK-style csproj with Microsoft.VSSDK.BuildTools providing VSIX packaging.
    NEEDS VERIFICATION: exact package versions against the VS 17.x that will build this.
  -->

  <PropertyGroup>
    <ProjectGuid>{B3C3A2E4-7F1D-4D3A-9A1A-5E4F1A2B3C4D}</ProjectGuid>
    <TargetFramework>net472</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <UseWPF>true</UseWPF>
    <GeneratePkgDefFile>true</GeneratePkgDefFile>
    <IncludeAssemblyInVSIXContainer>true</IncludeAssemblyInVSIXContainer>
    <IncludeDebugSymbolsInVSIXContainer>false</IncludeDebugSymbolsInVSIXContainer>
    <IncludeDebugSymbolsInLocalVSIXDeployment>true</IncludeDebugSymbolsInLocalVSIXDeployment>
    <CopyBuildOutputToOutputDirectory>true</CopyBuildOutputToOutputDirectory>
    <CopyOutputSymbolsToOutputDirectory>true</CopyOutputSymbolsToOutputDirectory>
    <CreateVsixContainer>true</CreateVsixContainer>
    <DeployExtension>true</DeployExtension>
    <RootNamespace>VSFilterText</RootNamespace>
    <AssemblyName>VSFilterText</AssemblyName>
  </PropertyGroup>

  <!-- F5 launches Visual Studio under the Experimental hive so the extension loads in
       isolation from the main VS install. $(DevEnvDir) is set by MSBuild when building
       inside VS and points at the running devenv.exe. -->
  <PropertyGroup Condition="'$(Configuration)' == 'Debug'">
    <StartAction>Program</StartAction>
    <StartProgram>$(DevEnvDir)devenv.exe</StartProgram>
    <StartArguments>/rootsuffix Exp</StartArguments>
  </PropertyGroup>

  <!-- Floating 18.* picks up the latest VS 18-targeted package at restore time.
       If the floating range doesn't resolve, pin to a concrete 18.x version reported
       by `nuget list Microsoft.VSSDK.BuildTools -AllVersions`. -->
  <ItemGroup>
    <PackageReference Include="Microsoft.VSSDK.BuildTools" Version="18.*" PrivateAssets="all" />
    <PackageReference Include="Microsoft.VisualStudio.SDK" Version="18.*" ExcludeAssets="runtime" />
  </ItemGroup>

  <ItemGroup>
    <Reference Include="System.ComponentModel.Composition" />
    <Reference Include="System.Design" />
  </ItemGroup>

  <ItemGroup>
    <None Include="source.extension.vsixmanifest">
      <SubType>Designer</SubType>
    </None>
  </ItemGroup>

  <ItemGroup>
    <VSCTCompile Include="Commands\VSFilterTextPackage.vsct">
      <ResourceName>Menus.ctmenu</ResourceName>
    </VSCTCompile>
  </ItemGroup>

  <ItemGroup>
    <Page Update="Editor\FilterQueryMargin.xaml">
      <Generator>MSBuild:Compile</Generator>
      <SubType>Designer</SubType>
    </Page>
  </ItemGroup>

</Project>
